import type { SiteData } from '@/types/site';import { Button,Card } from '@/components/ui';export function ExportPanel({data,onExportZip}:{data:SiteData;onExportZip:()=>void}){return <Card><h2 className="text-xl font-black">匯出靜態網站 ZIP</h2><p className="mt-2 text-slate-600">ZIP 會包含 index.html、assets/、siteData.json 與 README.txt。解壓縮後可直接打開 index.html。</p><Button className="mt-4" onClick={onExportZip}>下載 generated-site.zip</Button></Card>}
