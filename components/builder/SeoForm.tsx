import type { SiteData } from '@/types/site';import { Card,Input,Textarea,Select } from '@/components/ui';export function SeoForm({data,onChange}:{data:SiteData;onChange:(d:SiteData)=>void}){return <Card className="grid gap-4"><h2 className="text-xl font-black">SEO 設定</h2><Input label="網站標題" value={data.seo.title} onChange={e=>onChange({...data,seo:{...data.seo,title:e.target.value}})}/><Textarea label="Meta 描述" value={data.seo.description} onChange={e=>onChange({...data,seo:{...data.seo,description:e.target.value}})}/><Input label="URL Slug" value={data.seo.slug} onChange={e=>onChange({...data,seo:{...data.seo,slug:e.target.value}})}/><Select label="OG 圖片" value={data.seo.ogImageId||''} onChange={e=>onChange({...data,seo:{...data.seo,ogImageId:e.target.value||undefined}})}><option value="">未指定</option>{data.media.map(m=><option key={m.id} value={m.id}>{m.name}</option>)}</Select></Card>}
