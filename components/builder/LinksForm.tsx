import type { SiteData } from '@/types/site';import { Card,Input } from '@/components/ui';const fields:[keyof SiteData['links'],string][]= [['line','LINE'],['instagram','Instagram'],['facebook','Facebook'],['threads','Threads'],['tiktok','TikTok'],['googleMap','Google Maps'],['ubereats','Uber Eats'],['foodpanda','foodpanda'],['orderForm','訂購表單'],['reservation','訂位連結']];export function LinksForm({data,onChange}:{data:SiteData;onChange:(d:SiteData)=>void}){return <Card className="grid gap-4"><h2 className="text-xl font-black">連結設定</h2>{fields.map(([k,l])=><Input key={k} label={l} value={data.links[k]||''} onChange={e=>onChange({...data,links:{...data.links,[k]:e.target.value||undefined}})}/>)}</Card>}
