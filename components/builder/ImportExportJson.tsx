import type { SiteData } from '@/types/site';import { Button,Card } from '@/components/ui';import { exportSiteDataAsJson,importSiteDataFromJson } from '@/lib/storage';export function ImportExportJson({data,onImport}:{data:SiteData;onImport:(d:SiteData)=>void}){return <Card><h2 className="text-xl font-black">匯入 / 匯出 JSON</h2><div className="mt-4 flex flex-wrap gap-2"><Button onClick={()=>exportSiteDataAsJson(data)}>匯出 siteData.json</Button><label className="cursor-pointer rounded-xl border border-slate-200 bg-white px-4 py-2 text-sm font-semibold"><input type="file" accept="application/json" hidden onChange={e=>{const f=e.target.files?.[0];if(f)importSiteDataFromJson(f).then(onImport).catch(()=>alert('JSON 格式錯誤'))}}/>匯入 siteData.json</label></div></Card>}
