import type { SiteData } from '@/types/site';import { Card,Toggle } from '@/components/ui';const labels:Record<keyof SiteData['modules'],string>={hero:'首頁主視覺 Hero',featuredProducts:'招牌商品',menu:'菜單區塊',brandStory:'品牌故事',storeInfo:'門市資訊',map:'Google Maps',faq:'FAQ',socialLinks:'社群連結',footer:'Footer'};export function ModuleToggleForm({data,onChange}:{data:SiteData;onChange:(d:SiteData)=>void}){return <Card className="grid gap-3"><h2 className="text-xl font-black">功能模組</h2>{(Object.keys(labels) as (keyof SiteData['modules'])[]).map(k=><Toggle key={k} label={labels[k]} checked={data.modules[k]} onChange={v=>onChange({...data,modules:{...data.modules,[k]:v}})}/>)}</Card>}
