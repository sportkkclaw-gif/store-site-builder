import JSZip from 'jszip';import type { SiteData } from '@/types/site';import { exportStaticSite } from './exportStaticSite';import { dataUrlToFile } from './imageUtils';
export async function exportZip(data:SiteData){const zip=new JSZip();zip.file('index.html',exportStaticSite(data));zip.file('siteData.json',JSON.stringify(data,null,2));zip.file('README.txt','這是由 StoreSite Builder 產生的靜態網站。\n請將 index.html 與 assets 資料夾一起上傳到任何靜態網站主機。\n若要修改內容，請回到 StoreSite Builder 匯入 siteData.json 編輯後重新匯出。');const assets=zip.folder('assets')!;data.media.forEach(m=>{const f=dataUrlToFile(m.dataUrl);assets.file(`${m.id}.${f.ext}`,f.buffer,{base64:true})});const blob=await zip.generateAsync({type:'blob'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='generated-site.zip';a.click();URL.revokeObjectURL(url)}
